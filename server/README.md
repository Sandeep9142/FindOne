# FindOne Server

Backend stack:

- Node.js
- Express.js
- MongoDB
- Mongoose

Current status:

- database-ready backend foundation created
- Mongoose models added for the first project entities
- Mongo connection and category seed script included

Run flow:

1. create `server/.env`
2. install dependencies
3. start MongoDB
4. run seed script if needed
5. start the server
